# Vulnerable MCP Server Dependencies
# 이 파일은 교육 목적으로만 사용되어야 합니다.

# MCP 서버 실행에 필요한 기본 라이브러리들
# Python 표준 라이브러리만 사용하므로 추가 의존성은 없습니다.
# subprocess, asyncio, json 등은 Python 표준 라이브러리에 포함되어 있습니다.

# 참고: 실제 MCP 서버에서는 mcp 라이브러리를 사용하는 것이 권장됩니다.
# pip install mcp